services:
  ai_planner:
    build: .
    network_mode: host
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
      - ROS_DOMAIN_ID=0
    volumes:
      - ./src:/ros2_ws/src
    ipc: host
    stdin_open: true
    tty: true
